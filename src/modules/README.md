Legislacao module

- `Legislacao.tsx` exports a small `LegislacaoList` component and `sampleLegislacao()` helper.
- Purpose: simple UI to list, search and perform placeholders for CRUD operations on legal items.

Next steps:
- Wire to API routes for persistent storage.
- Implement edit/create forms and confirmation dialogs.
