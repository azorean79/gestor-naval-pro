const fs = require("fs"); let code = fs.readFileSync("src/app/api/search/global/route.ts", "utf8"); code = code.replace(/const results = \[[\s\S]*?\];/, "const results = [\n      ...jangadas.map(j => ({ type: \"Jangada\", label: `${j.serial} (${j.brand})`.trim(), href: `/jangadas/${j.id}` })),\n      ...navios.map(n => ({ type: \"Navio\", label: n.nome, href: `/navios/${n.id}` })),\n      ...clientes.map(c => ({ type: \"Cliente\", label: c.nome, href: `/clientes/${c.id}` }))\n    ];"); fs.writeFileSync("src/app/api/search/global/route.ts", code); console.log("OK");
